<nav class="navbar navbar-inverse navbar-fixed-top">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="<%= root_url %>">
				<%= image_tag('planet-saturn.png', size: "50x50", alt: "ELI5ience Home")  %>
			</a> 
		</div>
		<div id="navbar" class="navbar-collapse collapse">
			<form class="navbar-form navbar-right">
				<% if signed_in? %>
					<div class="header-profile">
						<%= link_to "Welcome, " + current_user.username, '/#/users/' + current_user.id.to_s %>
					</div>
					<div class="header-sign-out">
						<%= link_to( "Sign Out", session_url, method: :delete)%><br>
					</div>
				<% else %>
					<div class="header-sign-out">
						<%= link_to "Log In / Guest", new_session_url %>
					</div>
					<div class="header-profile">
						<%= link_to "Create Account", new_user_url %>
					</div>
				<% end %>
			</form>
		</div><!--/.navbar-collapse -->
	</div>
</nav>
<br>
<br>
